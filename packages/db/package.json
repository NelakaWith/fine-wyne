{
  "name": "@fine-wyne/db",
  "version": "1.0.0",
  "description": "Database package for fine-wyne",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^6.19.0",
    "typescript": "^5.0.0"
  }
}
